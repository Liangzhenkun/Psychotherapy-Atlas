name: Repo Guard

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  repo-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run repo guard
        shell: pwsh
        run: ./scripts/repo_guard.ps1 -Mode repo
