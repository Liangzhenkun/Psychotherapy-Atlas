# Download Notes

- `NICE_NG222_Landing.html` was saved successfully as a local reference copy of the guideline landing page.
- The APA guideline site returned an anti-bot protection page instead of the actual PDF, so no local APA PDF was kept in this folder.
- Use the `.url` shortcuts and the links in `reference_index.csv` for the most reliable access path.
